// Game over screen is handled inline via DOM manipulation in combat.ts (damagePlayer).
// The restart button uses onclick="location.reload()" in the HTML.
// This module exists as a placeholder for potential future game-over logic.

export function setupGameOver(): void {
  // The restart button is wired via onclick in the HTML template.
  // No additional setup needed.
}
